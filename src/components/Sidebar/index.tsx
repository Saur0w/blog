import styles from "./style.module.scss";

export default function Sidebar() {
  return (
    <aside className={styles.sidebar}>
      <nav className={styles.nav}>
        {/* Sidebar structure - content will be added later */}
      </nav>
    </aside>
  );
}